type: markdown
content: >-
  ### 🔬 Résultats détaillés


  | Paramètre | Valeur | Référence |

  | :--- | ---: | ---: |{% for param in state_attr('sensor.water_quality',
  'parameters') %}

  | {{ param.name }} | **{{ param.value }}** {% if param.unit and param.unit !=
  'SANS OBJET' %}{{ param.unit }}{% endif %} | {{ param.reference if
  param.reference and param.reference != 'N/A' else '-' }} |{% endfor %}
